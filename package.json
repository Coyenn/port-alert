{
  "name": "port-alert",
  "version": "0.0.1",
  "description": "Listens on a port and sends a notification when it is sent a request",
  "main": "src/index.ts",
  "scripts": {
    "watch": "concurrently \"tsc -w\" \"nodemon dist/index.js\"",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "keywords": [
    "port",
    "alert",
    "scanner"
  ],
  "author": "Tim Ritter <t-ritter-mail@web.de>",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.5.0",
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.1",
    "typescript": "^5.1.6"
  }
}
